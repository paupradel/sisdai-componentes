import {
  //NarrativaScroll,
  ComponenteVacio,
} from './components'

const plugin = {
  install: function (Vue) {
    //Vue.use(NarrativaScroll)
    Vue.use(ComponenteVacio)
  },
}

export default plugin
